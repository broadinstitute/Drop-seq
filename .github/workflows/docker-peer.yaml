# based on:
# https://github.com/broadinstitute/Drop-seq/new/master?filename=.github%2Fworkflows%2Fdocker-publish.yml&workflow_template=ci%2Fdocker-publish
name: PEER Docker

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

on:
  push:
    branches: [ "master" ]
    # Publish semver tags as releases.
    tags: [ 'v*.*.*' ]
  pull_request:
    branches: [ "master" ]
    paths:
      - '.github/workflows/docker-drop-seq_reusable.yaml'
      - '.github/workflows/docker-peer.yaml'
      - 'src/R/PEER/**'
      - 'src/docker/PEER/**'
  workflow_dispatch:

jobs:
  docker-drop-seq_r:
    uses: ./.github/workflows/docker-drop-seq_reusable.yaml
    with:
      registry: quay.io
      image_name: broadinstitute/peer
      dockerfile: src/docker/PEER/Dockerfile
    secrets: inherit
    permissions:
      contents: read
